import { DeliveryMessage } from './delivery-message';
import { AngularFirestore } from '@angular/fire/firestore';

describe('DeliveryMessage', () => {
  it('should create an instance', () => {
    expect(new DeliveryMessage().toBeTruthy();
  });
});
